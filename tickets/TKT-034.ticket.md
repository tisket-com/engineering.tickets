---
status: resolved
priority: medium
created: 2026-02-04
assignee: @james
---
# TKT-034: Replace payrun sidebar navigation with horizontal tabs

Replace the payrun sidebar navigation (`SecondarySidebar`) with horizontal tabs matching the employee page pattern (`employee-tabs.tsx`).

**Changes:**
- Created `payrun-tabs.tsx` component with header (back link, payrun name, pay date/frequency/paygroup subtitle) and 3 always-visible tabs (HR Changes, Payrun, Payments)
- Unreached stages render as disabled tabs (not clickable, visually dimmed)
- Updated `layout.tsx` to use centered `max-w-5xl` layout with `PayrunTabs` instead of `SecondarySidebar`
- Removed `getPayrunItems()` from `lib/navigation.ts`
